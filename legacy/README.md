# Legacy

Not part of Electronics v1 production. Kept for provenance; do not import from production modules.

- `benchmark_export/` — old Case-tree exporter (not `scripts/package_market_cases.py`)
- `benchmark_split/` — old Case-key split
- `case_build_population/` — GT2 `case_users` / population sampling
